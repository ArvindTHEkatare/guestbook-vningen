<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Hantera Gästboksinlägg</title>
    <style>
        body {
            background-color: rgb(219, 204, 181);
        }
        h1 {
            font-family: "Sacramento", serif;
            font-weight: 400;
            font-style: normal;
        }
        h2 {
            font-family: "Sacramento", serif;
            font-weight: 200;
            font-style: normal;
        }
        label {
            font-family: "Alice", serif;
            font-weight: 400;
            font-size: normal;
        }
        #name {
            width: 200px;
            height: 20px;
        }
        #message {
            width: 200px;
            height: 150px;
        }
        button {
            background-color: royalblue;
            color: white;
            border: 2px solid royalblue;
            padding: 12px 24px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 8px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        button:hover {
            background-color: royalblue;
            border-color: royalblue;
            transform: scale(1.2);
        }
        button:active {
            background-color: royalblue;
            transform: scale(1);
        }
        #update {
            width: 300px;
            height: 40px;
        }

        #Uppdatera{
            margin-top: 10px;
            display: block;
        }
        /* div {
            display: block;
            margin-bottom: 10px;
        } */
        
        h2 {
            display: block; 
            margin-bottom: 10px;
            
        }
        #inputfield{
            width: 250px;
            height: 40px;
        }

    form {
        display: block; 
        margin-bottom: 10px;
    }
    </style>
</head>
<body>
        <img src="/linje.png" alt="">
        <h1>Admin - Hantera Gästboksinlägg</h1>
 
        <ul>
            <% guestbook.forEach(entry => { %>
            <li>
                <h2><%= entry.namn %>: <%= entry.meddelande %></h2>
     
                <form action="/update/<%= entry.id %>" method="POST">
                    <input id="inputfield" type="text" name="newMessage" value="<%= entry.meddelande %>" required>
                    <button id="Uppdatera" type="submit">Uppdatera</button>
                </form>
                <form action="/delete/<%= entry.id %>" method="POST">
                    <button id="tabort" type="submit">Ta bort</button>
                </form>
       
            </li>
            <br />
            <% }); %>
        </ul>
        <img src="/linje.png" alt="">
        <h1>Återvänd till startsidan:</h1>
        <form action="/" method="GET">
            <button type="submit">Till startsidan</button>
        </form>
        <img src="/linje.png" alt="">
</body>
</html>